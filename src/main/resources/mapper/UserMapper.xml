<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.Zewang.myBlog.dao.UserMapper">

  <resultMap id="BaseResultMap" type="org.Zewang.myBlog.model.User">
    <id column="id" property="id" jdbcType="BIGINT"/>
    <result column="username" property="username" jdbcType="VARCHAR"/>
    <result column="password" property="password" jdbcType="VARCHAR"/>
    <result column="role" property="role" jdbcType="VARCHAR"/>
  </resultMap>

  <sql id="Base_Column_List">
    id, username, password, role
  </sql>

  <select id="findByUsername" resultMap="BaseResultMap" parameterType="java.lang.String">
    SELECT
    <include refid="Base_Column_List"/>
    FROM user
    WHERE username = #{username,jdbcType=VARCHAR}
  </select>

  <insert id="insert" parameterType="org.Zewang.myBlog.model.User" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO user
      (username, password, role)
    VALUES
      (#{username,jdbcType=VARCHAR},
       #{password,jdbcType=VARCHAR},
       #{role,jdbcType=VARCHAR})
  </insert>

  <select id="existsByUsername" resultType="boolean">
    SELECT COUNT(1) > 0
    FROM user
    WHERE username = #{username,jdbcType=VARCHAR}
  </select>

</mapper>
